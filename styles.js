import { StyleSheet } from "react-native";

export const sharedStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },

  titleBold: {
    fontSize: 20,
    fontWeight: "bold",
  },

  primaryBold: {
    fontSize: 16,
    fontWeight: "bold",
  },

  secondaryBold: {
    fontSize: 14,
    fontWeight: "bold",
  },
  secondaryNormal: {
    fontSize: 14,
  },
});
